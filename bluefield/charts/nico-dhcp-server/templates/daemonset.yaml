apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "nico-dhcp-server.fullname" . }}
  labels:
    {{- include "nico-dhcp-server.labels" . | nindent 4 }}
    {{- with .Values.serviceDaemonSet.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- with .Values.serviceDaemonSet.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- with .Values.serviceDaemonSet.updateStrategy }}
  updateStrategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "nico-dhcp-server.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.serviceDaemonSet.annotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "nico-dhcp-server.selectorLabels" . | nindent 8 }}
        {{- with .Values.serviceDaemonSet.labels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      dnsPolicy: ClusterFirst
      {{- if not (empty .Values.serviceDaemonSet.nodeSelector) }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
              {{- toYaml .Values.serviceDaemonSet.nodeSelector | nindent 12 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: nico-dhcp-server
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          # The shipped image provides /busybox/sh. Keep the pod-specific bind;
          # Rust socket addresses require brackets only for IPv6.
          command:
            - /busybox/sh
            - -ec
            - |
              listen_host="$POD_IP"
              case "$listen_host" in
                *:*) listen_host="[$listen_host]" ;;
              esac
              exec "$1" "--grpc-listen-addr=${listen_host}:10079" "--metrics-listen-addr=${listen_host}:10080"
            - --
          args:
            - /var/support/forge-dhcp/bin/forge-dhcp-server
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with toYaml .Values.serviceDaemonSet.resources }}
          resources:
            limits:
              {{- . | nindent 14 }}
            requests:
              {{- . | nindent 14 }}
          {{- end }}
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP  # Always resolves to cluster-internal IP
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: RUST_BACKTRACE
              value: "full"
          volumeMounts:
            - name: dhcp-conf
              mountPath: /var/support/forge-dhcp/conf
            - name: dhcp-logs
              mountPath: /var/support/forge-dhcp/logs
      volumes:
        - name: dhcp-conf
          hostPath:
            path: /var/lib/hbn/var/support/forge-dhcp/conf
            type: DirectoryOrCreate
        - name: dhcp-logs
          hostPath:
            path: /var/lib/hbn/var/support/forge-dhcp/logs
            type: DirectoryOrCreate
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
