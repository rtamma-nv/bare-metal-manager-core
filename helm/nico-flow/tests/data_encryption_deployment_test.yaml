# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

suite: Flow data encryption key mount
templates:
  - configmap.yaml
  - deployment.yaml
tests:
  - it: mounts the key read-only in the Flow container only
    template: deployment.yaml
    asserts:
      - lengthEqual:
          path: spec.template.spec.containers
          count: 1
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: FLOW_DATA_ENCRYPTION_KEY_PATH
            value: /var/run/secrets/flow-data-encryption/encryption-key
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: data-encryption-key
            mountPath: /var/run/secrets/flow-data-encryption
            readOnly: true
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data-encryption-key
            secret:
              secretName: flow-data-encryption-key
              items:
                - key: flow-data-encryption-key
                  path: encryption-key

  - it: mounts an externally managed Secret
    template: deployment.yaml
    set:
      dataEncryption.key.existingSecret: externally-managed-flow-key
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data-encryption-key
            secret:
              secretName: externally-managed-flow-key
              items:
                - key: flow-data-encryption-key
                  path: encryption-key

  - it: mounts the chart-managed key when a reused 0.1.0 release carries no dataEncryption block
    template: deployment.yaml
    set:
      dataEncryption: null
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data-encryption-key
            secret:
              secretName: flow-data-encryption-key
              items:
                - key: flow-data-encryption-key
                  path: encryption-key

  - it: mounts the chart-managed key when reused values carry a null dataEncryption.key
    template: deployment.yaml
    set:
      dataEncryption.key: null
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data-encryption-key
            secret:
              secretName: flow-data-encryption-key
              items:
                - key: flow-data-encryption-key
                  path: encryption-key
